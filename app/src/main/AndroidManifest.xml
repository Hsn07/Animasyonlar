<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hsn07.animasyonlar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Anasayfa">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Alpha" />
        <activity android:name=".Rotate" />
        <activity android:name=".Translate" />
        <activity android:name=".Scale" />
        <activity android:name=".Karisik" />
        <activity android:name=".SayfaGecisi" />
        <activity android:name=".DarawableAnimation"></activity>
    </application>

</manifest>